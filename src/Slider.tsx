import React from "react";

const Slider = () => {
  return (
    <div className="slider">
      <p>This will be for the slider once cards on the gallery are clicked this will zoom in and have and exit button and arrows to go to the next image it will also look like a large card and take the whole page and the back grould cards blurred....blog detail like</p>
    </div>
    );
}
 
export default Slider;